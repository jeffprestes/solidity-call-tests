# See more config options https://github.com/foundry-rs/foundry/tree/master/config[rpc_endpoints]
[profile.default]
src = 'src'
out = 'out'
libs = ['lib']

mumbai = "${RPC_URL}"

[profile.polygonscan]
mumbai = { key = "${POLYGONSCAN_API_KEY}" }

[etherscan]
mumbai = { key = "${POLYGONSCAN_API_KEY}" }